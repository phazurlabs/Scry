# Demo (placeholder)

A terminal recording of `scry init` → audit → a blocked skill action will live
here.

To record it once asciinema is wired up:

```bash
asciinema rec docs/demo.cast \
  -c "npx @phazur/scry audit fixtures/malicious/network-egress"
```

Until then, the before/after report in the [README](../README.md#before--after)
shows the same output verbatim.
